# Platform infrastructure — environment template
# Copy to .env and fill in real values.
# Never commit .env to git.

# PostgreSQL superuser password
POSTGRES_PASSWORD=

# Cloudflare Tunnel token
# Obtained from: Cloudflare Zero Trust → Networks → Tunnels → shogun-laptop → Configure → Token
TUNNEL_TOKEN=
